# Sample corpus

Phase 1 ships a seed script that ingests one file per modality here, so a fresh
clone is queryable in about two minutes:

- a text-layer PDF
- a scanned PDF, to exercise the OCR path
- an image
- a short mp3
- a short mp4

Files are not committed; `pnpm seed` fetches or generates them.
